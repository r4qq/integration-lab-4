# integration-lab-4

### golang-docker

Celem laboratorium jest przygotowanie aplikacji do pracy w środowisku izolowanym przy użyciu Dockera. 

Przed uruchomieniem należy pobrać zależności:
```bash
go mod download
```

Uruchomienie projektu:
```bash
go run main.go
```